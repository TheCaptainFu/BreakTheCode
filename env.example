# Break The Code - Environment Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# Security Settings
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
GAME_RATE_LIMIT_MAX=30

# CORS Settings (for production)
ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: Database URL (if you add persistence later)
# DATABASE_URL=your_database_connection_string

# Optional: Session Secret (if you add user sessions)
# SESSION_SECRET=your_super_secret_session_key_here_make_it_long_and_random 